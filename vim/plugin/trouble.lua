require("trouble").setup {
  auto_close = false,
  auto_open = false,
  auto_preview = true,
  focus = true,
  icons = {
    folder_closed = " ",
    folder_open = " ",
  },
  modes = {
    diagnostics = {
      auto_open = false,
      auto_close = false,
    },
  },
}
